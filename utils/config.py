class Config:
    BASE_URL = "https://travelbuddy.com"  # Update with your website's URL
    BROWSER = "chrome"  # Options: "chrome", "firefox", etc.
    TIMEOUT = 10  # Default timeout for waiting elements

    # Credentials (use environment variables in production)
    USERNAME = "test_user"
    PASSWORD = "test_password"
